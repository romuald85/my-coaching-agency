{% extends 'base.html.twig' %}

{% block title article.title %}

{% block body %}
<article>
    <h1>{{article.title}}</h1>
    <p>{{article.description}}</p>
</article>
<a href="{{path('app_article')}}">Retour</a>
{% endblock %}
