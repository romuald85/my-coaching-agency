{% for type, messages in app.flashes(['success', 'danger', 'info']) %}
    {% for message in messages %}
        <div class="mt-5 px-3 py-3 mx-auto text-center alert alert-{{type}} text-center" role="alert">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}
