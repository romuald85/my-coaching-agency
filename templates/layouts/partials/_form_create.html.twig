{{form_start(form)}}

    {{form_widget(form)}}

    <input type="submit" class="btn btn-primary btn-block" value="{{submitButtonText|default('Créer article')}}" formnovalidate>
    {{form_end(form)}}